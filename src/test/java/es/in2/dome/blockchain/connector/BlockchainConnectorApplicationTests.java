/*
package es.in2.dome.blockchain.connector;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit.jupiter.SpringJUnitConfig;

import static org.hibernate.validator.internal.util.Contracts.assertNotNull;

@SpringBootTest
@SpringJUnitConfig(BlockchainConnectorApplication.class)
class BlockchainConnectorApplicationTests {

	@Test
	void contextLoads() {
		// Start the application
		BlockchainConnectorApplication.main(new String[]{});
		// Add the assertion to check if the application context loads successfully
		assertNotNull(BlockchainConnectorApplication.class);
	}

}
*/